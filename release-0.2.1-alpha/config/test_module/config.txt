# Test Module Configuration
# This is a simple test file to demonstrate the auto-discovery system

[settings]
enabled = true
debug = false
theme = "dark"

[paths]
data_dir = "~/.local/share/test_module"
cache_dir = "~/.cache/test_module"

[features]
auto_update = true
notifications = true
logging = true

# This file should be copied to ~/.config/test_module/config.txt
# when the test_module is installed via configz
